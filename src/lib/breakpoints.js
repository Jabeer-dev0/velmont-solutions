/** Shared layout breakpoints — keep in sync with site.css @media rules. */
export const BP_TABLET = 920;

export const MQ_TABLET_MAX = `(max-width: ${BP_TABLET}px)`;
export const MQ_DESKTOP_MIN = `(min-width: ${BP_TABLET + 1}px)`;
